POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=6500
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password123
POSTGRES_DB=qash
POSTGRES_DB_SSL=false
POSTGRES_SCHEMA=public

# Dynamic DATABASE_URL (constructed from above values)
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public&sslmode=${POSTGRES_DB_SSL === 'true' ? 'require' : 'prefer'}"