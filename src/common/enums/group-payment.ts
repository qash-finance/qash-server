export enum GroupPaymentStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  EXPIRED = 'expired',
}

export enum GroupPaymentMemberStatus {
  PENDING = 'pending',
  PAID = 'paid',
  DENIED = 'denied',
}
